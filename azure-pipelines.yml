trigger:
  - main

pool:
  vmImage: ubuntu-latest

steps:
  - task: Docker@2
    displayName: Build and Push Docker image
    inputs:
      command: buildAndPush
      containerRegistry: labarq-rag-acr
      repository: labarq-rag
      Dockerfile: Dockerfile
      tags: |
        $(Build.BuildId)
        latest

